# 🚀 Быстрый старт

## Минимальные шаги для запуска

### 1. Установка зависимостей

**Backend:**
```bash
cd backend
pip install -r requirements.txt
```

**Frontend:**
```bash
cd frontend
npm install
```

### 2. Запуск

**Терминал 1 (Backend):**
```bash
cd backend
python app.py
```

**Терминал 2 (Frontend):**
```bash
cd frontend
npm run dev
```

### 3. Открыть в браузере

Перейдите на: **http://localhost:5173**

---

## Первое использование

1. Загрузите DXF файлы через интерфейс
2. Проверьте количество деталей
3. Выберите материал и укажите цену
4. Нажмите "Рассчитать раскрой"
5. Экспортируйте результаты в Excel или PDF

---

## Решение проблем

**Backend не запускается:**
- Проверьте версию Python: `python --version` (нужна 3.8+)
- Убедитесь, что все зависимости установлены: `pip list`

**Frontend не запускается:**
- Проверьте версию Node.js: `node --version` (нужна 16+)
- Удалите `node_modules` и установите заново: `rm -rf node_modules && npm install`

**Ошибки при загрузке файлов:**
- Проверьте формат файлов (должны быть .dxf)
- Убедитесь, что backend запущен на порту 5000

---

## Полезные команды

```bash
# Проверить версию приложения
curl http://localhost:5000/api/version

# Проверить статус backend
curl http://localhost:5000/
```
