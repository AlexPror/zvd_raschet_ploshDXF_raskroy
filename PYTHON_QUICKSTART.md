# 🐍 Быстрая установка Python для Electron

## ✅ Что сделано:

1. ✅ Обновлен `electron/main.js` - теперь использует встроенный Python
2. ✅ Обновлен `frontend/package.json` - Python включен в `extraResources`
3. ✅ Созданы скрипты для автоматической установки Python

## 🚀 Быстрая установка (Windows):

```bash
cd build
install_python.bat
```

Скрипт автоматически:
- Скачает Python 3.11.7 Embeddable (~15 MB)
- Установит pip
- Установит все зависимости backend (~50-100 MB)

**Время:** ~2-5 минут (зависит от скорости интернета)

## 📋 После установки Python:

1. **Соберите Electron приложение:**
   ```bash
   cd frontend
   npm run electron:build:win
   ```

2. **Результат:**
   - Установщик будет включать Python
   - Пользователям НЕ нужно устанавливать Python отдельно! 🎉

## 📦 Размер установщика:

- **Без Python:** ~50-80 MB
- **С Python:** ~120-200 MB

## ⚠️ Важно:

- Python должен быть в папке `build/python/` перед сборкой
- Если Python не найден, приложение попробует использовать системный Python
- Все зависимости должны быть установлены в `build/python/lib/site-packages/`

## 🔍 Проверка:

После установки Python проверьте:

```bash
cd build/python
python.exe --version
python.exe -c "import flask; print('OK')"
python.exe -c "import ezdxf; print('OK')"
```

---

**Готово!** Теперь можно собирать Electron приложение с встроенным Python! 🚀

Подробнее: [PYTHON_SETUP.md](PYTHON_SETUP.md)

