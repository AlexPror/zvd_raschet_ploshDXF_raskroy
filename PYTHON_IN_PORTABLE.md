# 🐍 Python в портативной версии

## Текущая ситуация

**Портативная версия НЕ содержит Python** - она использует системный Python, который должен быть установлен на компьютере бухгалтерии.

## ✅ Решение: Добавить Python в портативную версию

### Вариант 1: Автоматическая установка (РЕКОМЕНДУЕТСЯ)

1. **Установите Python в build папку:**
   ```bash
   cd build
   install_python.bat
   ```
   Это скачает и установит Python 3.11.7 Embeddable (~15 MB) + зависимости (~50-100 MB)

2. **Пересоберите портативную версию с Python:**
   ```bash
   .\add_python_to_build.bat
   ```

3. **Результат:**
   - Python будет включен в `resources/python/`
   - Бухгалтерии НЕ нужно устанавливать Python отдельно! 🎉

### Вариант 2: Ручная установка

1. **Скачайте Python Embeddable:**
   - https://www.python.org/ftp/python/3.11.7/python-3.11.7-embed-amd64.zip
   - Распакуйте в `build/python/`

2. **Установите pip:**
   ```bash
   cd build/python
   curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
   python.exe get-pip.py
   ```

3. **Установите зависимости:**
   ```bash
   python.exe -m pip install -r ../../backend/requirements.txt
   ```

4. **Пересоберите:**
   ```bash
   .\build_portable.bat
   ```

## 📦 Размер портативной версии

- **Без Python:** ~120 MB
- **С Python:** ~200-250 MB

## ✅ Проверка

После сборки проверьте:
```bash
dir frontend\dist-electron\win-unpacked\resources\python\python.exe
```

Если файл существует - Python включен! ✓

## 📋 Что изменилось

1. ✅ Обновлен `frontend/package.json` - Python добавлен в `extraResources`
2. ✅ Создан скрипт `add_python_to_build.bat` для автоматизации
3. ✅ `electron/main.js` уже настроен на использование встроенного Python

## ⚠️ Важно

- Python должен быть в `build/python/` **перед** сборкой
- Если Python не найден, приложение попробует использовать системный Python
- Все зависимости должны быть установлены в `build/python/lib/site-packages/`

---

**После добавления Python:** Бухгалтерии нужно только распаковать ZIP и запустить приложение! 🚀

