# 📐 ZVD Nesting Calculator - Web Version

**Веб-приложение для расчета площадей разверток и оптимизации раскроя листового материала**

[![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)](VERSION)
[![Python](https://img.shields.io/badge/python-3.8+-green.svg)](https://www.python.org/)
[![Vue.js](https://img.shields.io/badge/vue.js-3.4+-4FC08D.svg)](https://vuejs.org/)

---

## 🎯 Возможности

- ✅ Загрузка DXF файлов разверток
- ✅ Автоматический расчет размеров деталей
- ✅ Оптимизация раскроя на листах материала
- ✅ Визуализация размещения деталей
- ✅ Расчет стоимости с учетом цены материала
- ✅ Экспорт результатов в Excel и PDF
- ✅ Валидация раскроя (проверка пересечений и выходов за границы)
- ✅ Поддержка поворота деталей для оптимизации

---

## 📋 Требования

- **Python** 3.8 или выше
- **Node.js** 16 или выше
- **npm** или **yarn**

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/AlexPror/zvd_raschet_ploshDXF_raskroy.git
cd zvd_raschet_ploshDXF_raskroy
```

### 2. Установка зависимостей Backend

```bash
cd backend
pip install -r requirements.txt
```

### 3. Установка зависимостей Frontend

```bash
cd ../frontend
npm install
```

### 4. Запуск приложения

**В первом терминале (Backend):**
```bash
cd backend
python app.py
```

Backend будет доступен на `http://localhost:5000`

**Во втором терминале (Frontend):**
```bash
cd frontend
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

---

## 📖 Использование

### Основной рабочий процесс:

1. **Загрузка файлов**
   - Перетащите DXF файлы в область загрузки или выберите через кнопку
   - Программа автоматически определит размеры каждой детали

2. **Ввод данных**
   - Укажите номер заказа (опционально, можно извлечь из имени файла)
   - Выберите материал и укажите цену за м²
   - Проверьте количество деталей (автоматически извлекается из имени файла)

3. **Расчет раскроя**
   - Нажмите "Рассчитать раскрой"
   - Система оптимизирует размещение деталей на листах
   - Просмотрите визуализацию раскроя

4. **Экспорт результатов**
   - Экспорт в Excel: полные таблицы с данными
   - Экспорт в PDF: отчеты с визуализацией
   - Печать в PDF: через браузер

### Формат имен файлов DXF:

Рекомендуемый формат: `{номер} - {название} {количество}шт ({номер_заказа}).dxf`

Пример: `001 - Корпус короба прямой 1600 1шт (А-151025-1235).dxf`

Программа автоматически извлекает:
- Количество деталей из имени файла
- Номер заказа из скобок

---

## 📁 Структура проекта

```
zvd_nesting_web/
├── backend/              # Flask backend
│   ├── api/             # API endpoints
│   ├── utils/           # Утилиты (парсеры, оптимизатор)
│   ├── app.py           # Главный файл приложения
│   └── requirements.txt # Python зависимости
├── frontend/             # Vue.js frontend
│   ├── src/
│   │   ├── components/  # Vue компоненты
│   │   └── main.js      # Точка входа
│   └── package.json    # Node зависимости
├── README.md            # Этот файл
└── VERSION              # Версия приложения
```

---

## 🔧 Технологии

### Backend:
- **Flask** - веб-фреймворк
- **ezdxf** - парсинг DXF файлов
- **rectpack** - оптимизация раскроя
- **pandas/openpyxl** - экспорт в Excel
- **reportlab** - экспорт в PDF

### Frontend:
- **Vue.js 3** - фреймворк
- **Vuetify 3** - UI компоненты
- **Axios** - HTTP клиент
- **Vite** - сборщик

---

## 📦 Создание исполняемого файла (EXE)

### Вариант 1: PyInstaller (Backend + Frontend bundle)

```bash
# Установка PyInstaller
pip install pyinstaller

# Создание exe для backend
cd backend
pyinstaller --onefile --name "ZVD_Backend" app.py

# Для frontend нужно сначала собрать
cd ../frontend
npm run build
# Затем можно использовать Electron или другой инструмент
```

### Вариант 2: Electron (Рекомендуется для полного приложения)

```bash
# Установка Electron
npm install -g electron electron-builder

# Создание desktop приложения
cd frontend
npm install electron --save-dev
# Настроить electron-builder в package.json
```

### Вариант 3: Docker (Контейнеризация)

Создайте `Dockerfile` для удобного развертывания.

---

## 🌐 Деплой на GitHub Pages

GitHub Pages поддерживает только статические сайты. Для этого приложения нужен backend, поэтому:

### Варианты деплоя:

1. **Heroku** (рекомендуется для быстрого деплоя)
   - Поддержка Python и Node.js
   - Бесплатный тарифный план

2. **Railway** или **Render**
   - Простой деплой из GitHub
   - Автоматический деплой при push

3. **VPS** (DigitalOcean, AWS, etc.)
   - Полный контроль
   - Требует настройки сервера

4. **Docker + любой хостинг**
   - Универсальное решение
   - Легко масштабируется

---

## 🔄 Версионирование

Версия приложения хранится в файле `VERSION`.

Формат: `MAJOR.MINOR.PATCH`

- **MAJOR** - несовместимые изменения API
- **MINOR** - новая функциональность (обратно совместимо)
- **PATCH** - исправления ошибок

---

## 📝 Лицензия

[Укажите лицензию, если есть]

---

## 👤 Автор

AlexPror

---

## 🤝 Вклад в проект

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте требования (Python, Node.js версии)
2. Убедитесь, что все зависимости установлены
3. Проверьте логи в `backend/backend.log`
4. Откройте issue на GitHub

---

## 🎉 Благодарности

- Библиотека `rectpack` за алгоритм оптимизации раскроя
- Сообщество Vue.js и Vuetify за отличные инструменты
