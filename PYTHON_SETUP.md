# 🐍 Настройка встроенного Python для Electron

## Зачем это нужно?

Включение Python в установщик Electron позволяет:
- ✅ Пользователям не нужно устанавливать Python отдельно
- ✅ Гарантировать совместимость версий
- ✅ Упростить установку приложения

## Быстрая установка (Windows)

### Автоматический способ:

1. Запустите скрипт:
```bash
cd build
install_python.bat
```

Скрипт автоматически:
- Скачает Python 3.11.7 Embeddable
- Установит pip
- Установит все зависимости backend

### Ручной способ:

1. **Скачайте Python Embeddable:**
   - Перейдите: https://www.python.org/ftp/python/3.11.7/python-3.11.7-embed-amd64.zip
   - Или используйте другую версию 3.8+

2. **Распакуйте в `build/python/`:**
   ```
   build/python/
   ├── python.exe
   ├── python311.dll
   ├── python311.zip
   └── ...
   ```

3. **Установите pip:**
   ```bash
   cd build/python
   curl -L -o get-pip.py https://bootstrap.pypa.io/get-pip.py
   python.exe get-pip.py
   ```

4. **Установите зависимости:**
   ```bash
   python.exe -m pip install Flask Flask-CORS ezdxf openpyxl pandas PyPDF2 pdfplumber reportlab --target ./lib/site-packages
   ```

## macOS/Linux

Запустите скрипт:
```bash
cd build
chmod +x install_python.sh
./install_python.sh
```

Или вручную скопируйте системный Python в `build/python/`

## Проверка установки

```bash
cd build/python

# Windows
python.exe --version
python.exe -c "import flask; print('OK')"

# macOS/Linux
python3 --version
python3 -c "import flask; print('OK')"
```

## Размер

- Python embeddable: ~15-20 MB
- Зависимости: ~50-100 MB
- **Итого: ~70-120 MB** (увеличит размер установщика)

## После установки Python

1. **Соберите Electron приложение:**
   ```bash
   cd frontend
   npm run electron:build:win
   ```

2. **Проверьте размер установщика:**
   - Должен быть больше обычного (из-за Python)
   - Но пользователям не нужно устанавливать Python отдельно

## Структура в установщике

После сборки Python будет в:
```
resources/
├── python/          # Встроенный Python
│   ├── python.exe   # (Windows)
│   ├── lib/
│   └── ...
└── backend/         # Backend код
    └── app.py
```

## Устранение проблем

### Python не найден в установщике

1. Проверьте, что Python в `build/python/`
2. Проверьте `extraResources` в `package.json`
3. Проверьте логи сборки electron-builder

### Зависимости не работают

1. Убедитесь, что зависимости установлены в `lib/site-packages`
2. Проверьте `PYTHONPATH` в `electron/main.js`

### Большой размер установщика

Это нормально! Python + зависимости занимают ~70-120 MB.

Можно оптимизировать:
- Использовать Python 3.11 (меньше чем 3.12)
- Удалить ненужные модули из site-packages
- Использовать сжатие в electron-builder

## Альтернатива: Без встроенного Python

Если размер критичен, можно оставить без Python:
- Пользователи устанавливают Python отдельно
- Меньший размер установщика
- Но сложнее для пользователей

Для этого просто не добавляйте Python в `build/python/` и удалите из `extraResources` в `package.json`.

