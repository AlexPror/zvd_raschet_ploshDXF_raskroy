# ✅ Исправление API запросов в Electron

## Проблема
В Electron приложении запросы к `/api/upload` превращались в `file:///C:/api/upload` вместо `http://localhost:5000/api/upload`.

## Решение
Создан специальный конфигурационный файл для axios, который автоматически определяет окружение и использует правильный baseURL.

### Файлы изменены:
1. **`frontend/src/config/axios.js`** - новый файл с конфигурацией axios
2. **`frontend/src/components/AreaCalculator.vue`** - заменен `axios` на `apiClient`

### Как это работает:
- **В Electron:** Используется `http://localhost:5000` как baseURL
- **В браузере (dev):** Используются относительные пути, Vite proxy перенаправляет на `localhost:5000`

### Что нужно сделать:
1. Пересобрать frontend: `npm run build`
2. Пересобрать Electron: `.\build_portable.bat`
3. Запустить новую версию

### Проверка:
После запуска в консоли должно появиться:
- `🔌 Electron detected, using http://localhost:5000 for API` (в Electron)
- `🌐 Browser mode, using relative paths (Vite proxy)` (в браузере)

Теперь все API запросы будут идти на правильный адрес!

