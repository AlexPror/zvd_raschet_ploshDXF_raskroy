# 🔍 Проверка Python в архиве

## ❌ Текущая ситуация

**Python НЕ включен в ZIP архив.**

В архиве `ZVD_Nesting_Calculator_Portable.zip` содержится только:
- ✅ Приложение Electron
- ✅ Backend код (Python скрипты)
- ❌ Python интерпретатор (отсутствует)
- ❌ Установщик Python (отсутствует)

## ⚠️ Что это значит

Бухгалтерии нужно:
1. Установить Python 3.8+ с https://www.python.org/downloads/
2. При установке отметить "Add Python to PATH"
3. Установить зависимости:
   ```cmd
   cd "C:\Program Files\ZVD Nesting Calculator\resources\backend"
   py -m pip install -r requirements.txt
   ```

## ✅ Как включить Python в архив

### Шаг 1: Установите Python в build папку

```bash
cd build
install_python.bat
```

Это:
- Скачает Python 3.11.7 Embeddable (~15 MB)
- Установит pip
- Установит все зависимости (~50-100 MB)

### Шаг 2: Пересоберите портативную версию с Python

```bash
.\add_python_to_build.bat
```

Или вручную:
```bash
cd frontend
npm run build
npx electron-builder --win --config.win.target=dir --config.win.sign=null
```

### Шаг 3: Обновите ZIP архив

```bash
.\update_zip.bat
```

## 📦 Размер после включения Python

- **Без Python:** ~120 MB
- **С Python:** ~200-250 MB

## ✅ Проверка

После включения Python проверьте:

```bash
dir frontend\dist-electron\win-unpacked\resources\python\python.exe
```

Если файл существует - Python включен! ✓

## 🎯 Результат

После включения Python:
- ✅ Бухгалтерии НЕ нужно устанавливать Python отдельно
- ✅ Все зависимости уже установлены
- ✅ Приложение работает "из коробки"
- ⚠️ Размер архива увеличится до ~200-250 MB

---

**Текущий архив:** Python отсутствует, нужна установка в бухгалтерии  
**После включения Python:** Работает без дополнительной установки! 🚀

