# ⚡ Быстрая сборка Electron

## Готово к сборке! ✅

Все файлы настроены. Теперь можно собрать приложение.

## Команды для сборки

### Windows (рекомендуется):
```bash
cd frontend
npm run electron:build:win
```

### Все платформы:
```bash
cd frontend
npm run electron:build:all
```

## Что будет создано

После сборки в папке `frontend/dist-electron/` появится:
- **Windows**: `ZVD Nesting Calculator Setup 1.0.0.exe` (установщик)
- **macOS**: `ZVD Nesting Calculator-1.0.0.dmg`
- **Linux**: `ZVD Nesting Calculator-1.0.0.AppImage`

## Важно помнить

⚠️ **Python должен быть установлен** на системе, где будет запускаться приложение.

Пользователю нужно:
1. Установить Python 3.8+
2. Установить зависимости: `pip install -r backend/requirements.txt`
3. Запустить установщик Electron приложения

## Тестирование перед сборкой

Можно протестировать в режиме разработки:

```bash
# Терминал 1
cd backend
python app.py

# Терминал 2  
cd frontend
npm run dev

# Терминал 3
cd frontend
npm run electron:dev
```

---

**Готово к сборке!** Запустите команду выше. 🚀

