# 🚀 Инструкция по сборке Electron приложения

## Быстрый старт

### 1. Установите зависимости

```bash
cd frontend
npm install
```

### 2. Соберите frontend

```bash
npm run build
```

### 3. Соберите Electron приложение

**Для Windows:**
```bash
npm run electron:build:win
```

**Для всех платформ:**
```bash
npm run electron:build:all
```

### 4. Результат

Установщик будет в папке `frontend/dist-electron/`:
- **Windows**: `ZVD Nesting Calculator Setup 1.0.0.exe`
- **macOS**: `ZVD Nesting Calculator-1.0.0.dmg`
- **Linux**: `ZVD Nesting Calculator-1.0.0.AppImage`

---

## Режим разработки

Для тестирования приложения в режиме разработки:

**Терминал 1 - Backend:**
```bash
cd backend
python app.py
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

**Терминал 3 - Electron:**
```bash
cd frontend
npm run electron:dev
```

---

## Важные замечания

### ⚠️ Python должен быть установлен

Electron приложение требует, чтобы Python был установлен на системе пользователя.

Приложение автоматически найдет Python:
- Windows: использует `py` launcher
- macOS/Linux: использует `python3`

### 📦 Что включается в установщик

- Собранный frontend (папка `dist/`)
- Backend код (папка `backend/`)
- Electron runtime
- Все зависимости Node.js

**НЕ включается:**
- Python (должен быть установлен отдельно)
- Python зависимости (нужно установить вручную)

---

## Установка для пользователей

### Требования для пользователя:

1. **Python 3.8+** установлен и доступен в PATH
2. **Зависимости backend** установлены:
   ```bash
   pip install -r requirements.txt
   ```

### Альтернатива: Встроенный Python

Если нужно включить Python в установщик:

1. Скачайте Python embeddable для Windows
2. Распакуйте в `frontend/build/python/`
3. Обновите `electron/main.js` для использования встроенного Python

---

## Устранение проблем

### Backend не запускается

1. Проверьте Python: `python --version` или `py --version`
2. Установите зависимости: `pip install -r backend/requirements.txt`
3. Проверьте логи в консоли Electron (F12 или View → Toggle Developer Tools)

### Frontend не загружается

1. Убедитесь, что `npm run build` выполнен успешно
2. Проверьте наличие `frontend/dist/index.html`
3. Откройте DevTools для просмотра ошибок

### Ошибка при сборке

1. Убедитесь, что все зависимости установлены: `npm install`
2. Проверьте права доступа к папкам
3. Попробуйте очистить кэш: `rm -rf node_modules && npm install`

---

## Следующие шаги

1. ✅ Создайте иконки для приложения (см. `frontend/build/README.md`)
2. ✅ Протестируйте на целевой платформе
3. ✅ Создайте релиз на GitHub с установщиком
4. ✅ Добавьте инструкции по установке для пользователей

---

## Полезные команды

```bash
# Проверить версию Electron
npm list electron

# Проверить версию electron-builder
npm list electron-builder

# Очистить сборку
rm -rf frontend/dist frontend/dist-electron

# Пересобрать все
cd frontend
npm run build
npm run electron:build:win
```

---

Подробнее см. [ELECTRON_BUILD.md](ELECTRON_BUILD.md)

