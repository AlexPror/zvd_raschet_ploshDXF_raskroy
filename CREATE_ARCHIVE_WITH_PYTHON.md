# 📦 Создание архива С Python для бухгалтерии

## ✅ Правильная последовательность

**Важно:** Скрипты нужны ДЛЯ ВАС (для создания архива), а НЕ для бухгалтерии!

Бухгалтерии нужен только готовый ZIP архив с Python внутри.

---

## 🚀 Шаги для создания архива С Python

### Шаг 1: Установите Python в build папку

**Выполните на ВАШЕМ компьютере:**
```bash
cd build
install_python.bat
```

Это:
- Скачает Python 3.11.7 Embeddable
- Установит pip
- Установит все зависимости из `backend/requirements.txt`

**Время:** ~5-10 минут (зависит от интернета)

### Шаг 2: Пересоберите портативную версию с Python

**Выполните на ВАШЕМ компьютере:**
```bash
.\add_python_to_build.bat
```

Или вручную:
```bash
cd frontend
npm run build
npx electron-builder --win --config.win.target=dir --config.win.sign=null
```

**Результат:** Python будет включен в `win-unpacked\resources\python\`

### Шаг 3: Создайте ZIP архив

**Выполните на ВАШЕМ компьютере:**
```bash
.\update_zip.bat
```

Или вручную:
```bash
cd frontend\dist-electron
powershell Compress-Archive -Path win-unpacked\* -DestinationPath ZVD_Nesting_Calculator_Portable.zip -Force
```

---

## 📦 Что получится

**Файл для передачи:** `frontend\dist-electron\ZVD_Nesting_Calculator_Portable.zip`

**Размер:** ~200-250 MB (вместо ~120 MB)

**Содержимое:**
- ✅ Приложение Electron
- ✅ Backend код
- ✅ **Python интерпретатор** (в `resources/python/`)
- ✅ **Все зависимости Python** (уже установлены)

---

## ✅ Что передать бухгалтерии

**Только 2 файла:**
1. `ZVD_Nesting_Calculator_Portable.zip` (~200-250 MB)
2. `INSTALLATION_GUIDE.md` (инструкция)

**Бухгалтерии НЕ нужно:**
- ❌ Устанавливать Python
- ❌ Устанавливать зависимости
- ❌ Запускать скрипты

**Просто:**
1. Распаковать ZIP
2. Запустить `ZVD Nesting Calculator.exe`
3. Готово! 🎉

---

## 🔍 Проверка перед передачей

Убедитесь, что Python включен:

```bash
dir frontend\dist-electron\win-unpacked\resources\python\python.exe
```

Если файл существует - Python включен! ✓

---

## 📋 Итого

**Скрипты (`install_python.bat`, `add_python_to_build.bat`, `update_zip.bat`):**
- ✅ Нужны ДЛЯ ВАС (для создания архива)
- ❌ НЕ нужны бухгалтерии

**ZIP архив:**
- ✅ Содержит всё необходимое
- ✅ Python уже внутри
- ✅ Готов к использованию

---

**Выполните шаги 1-3 → получите архив с Python → передайте бухгалтерии!** 🚀

