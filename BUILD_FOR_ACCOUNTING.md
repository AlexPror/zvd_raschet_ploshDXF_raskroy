# 📦 Сборка установщика для бухгалтерии

## Быстрая сборка (без встроенного Python)

Если Python не скачивается автоматически, можно собрать установщик, который будет использовать системный Python.

### Шаг 1: Соберите frontend

```bash
cd frontend
npm run build
```

### Шаг 2: Соберите Electron установщик

```bash
cd frontend
npm run electron:build:win
```

### Шаг 3: Результат

Установщик будет в: `frontend/dist-electron/ZVD Nesting Calculator Setup 1.0.0.exe`

---

## Вариант с встроенным Python (рекомендуется)

Для полной автономности нужно установить Python:

1. **Скачайте Python вручную:**
   - https://www.python.org/ftp/python/3.11.7/python-3.11.7-embed-amd64.zip
   - Распакуйте в `build/python/`

2. **Установите зависимости:**
   ```bash
   cd build/python
   python.exe -m pip install Flask Flask-CORS ezdxf openpyxl pandas PyPDF2 pdfplumber reportlab --target ./lib/site-packages
   ```

3. **Соберите установщик:**
   ```bash
   cd frontend
   npm run electron:build:win
   ```

---

## Инструкция для бухгалтерии

Создайте файл `ИНСТРУКЦИЯ_ДЛЯ_БУХГАЛТЕРИИ.txt` с текстом ниже.

